package commands;

import org.apache.commons.cli.Option;
import org.apache.commons.cli.Options;

public class ViewRequest {
    public static Options viewRequestOptions () {

        Options viewRequestOptions = new Options();

        return viewRequestOptions;
    }
}
